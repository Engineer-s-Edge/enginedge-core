apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: main-node
  namespace: observability
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: enginedge
      component: main-node
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 15s

