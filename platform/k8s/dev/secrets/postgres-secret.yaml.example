## Example: PostgreSQL Secret
# DO NOT COMMIT real secrets. Copy this file to postgres-secret.yaml and fill in base64 values.
# How to use:
# 1) Copy:  cp k8s/secrets/postgres-secret.yaml.example k8s/secrets/postgres-secret.yaml
# 2) Choose a strong password, then base64 encode it:
#    echo -n 'your-postgres-password' | base64
# 3) Replace the placeholder below.
# 4) Apply: kubectl apply -f k8s/secrets/postgres-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: default
type: Opaque
data:
  # base64(password)
  postgres-password: <base64-postgres-password>
