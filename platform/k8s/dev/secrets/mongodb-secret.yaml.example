# This is an example secret file for MongoDB.
# DO NOT COMMIT THE ACTUAL SECRET FILE (mongodb-secret.yaml) TO VERSION CONTROL.
#
# How to use:
# 1. Copy this file: cp k8s/secrets/mongodb-secret.yaml.example k8s/secrets/mongodb-secret.yaml
# 2. Choose a secure username and password.
# 3. Base64 encode them. You can use the following commands:
#    echo -n 'your-username' | base64
#    echo -n 'your-password' | base64
# 4. Replace the placeholder values below with your new base64 encoded values.
# 5. Create the full MongoDB URI, including your new credentials, and base64 encode it:
#    echo -n 'mongodb://your-username:your-password@mongodb-service:27017/enginedge-main-node?authSource=admin' | base64
# 6. Replace the mongodb-uri placeholder below.
# 7. Apply the secret to your cluster: kubectl apply -f k8s/secrets/mongodb-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: default
type: Opaque
data:
  # Replace with your base64 encoded username
  mongodb-root-username: <your-base64-encoded-username>
  # Replace with your base64 encoded password
  mongodb-root-password: <your-base64-encoded-password>
  # Replace with your base64 encoded connection URI
  mongodb-uri: <your-base64-encoded-mongodb-uri>
