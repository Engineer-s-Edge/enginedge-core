## Example: MongoDB Replica Set Keyfile Secret
# DO NOT COMMIT real secrets. Copy this file to mongodb-keyfile-secret.yaml and fill in base64 values.
# How to use:
# 1) Generate a keyfile (must be exactly 1024 bytes for MongoDB):
#    openssl rand -base64 756 > mongodb-keyfile
#    truncate -s 1024 mongodb-keyfile
# 2) Base64 encode the keyfile:
#    cat mongodb-keyfile | base64 -w 0
# 3) Replace the placeholder below with the base64 encoded value
# 4) Apply: kubectl apply -f k8s/secrets/mongodb-keyfile-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: mongodb-keyfile-secret
  namespace: default
type: Opaque
data:
  # base64(mongodb-keyfile) - must be exactly 1024 bytes
  keyfile: <base64-keyfile>

