## Example: Kafka Secret
# DO NOT COMMIT real secrets. Copy this file to kafka-secret.yaml and fill in base64 values.
# How to use:
# 1) Copy:  cp k8s/secrets/kafka-secret.yaml.example k8s/secrets/kafka-secret.yaml
# 2) Choose a strong SASL password, then base64 encode it:
#    echo -n 'your-sasl-password' | base64
# 3) Replace the placeholder below.
# 4) Apply: kubectl apply -f k8s/secrets/kafka-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: kafka-secret
  namespace: default
type: Opaque
data:
  # base64(SASL password)
  sasl-password: <base64-sasl-password>
