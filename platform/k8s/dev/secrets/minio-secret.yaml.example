## Example: MinIO Secret
# DO NOT COMMIT real secrets. Copy this file to minio-secret.yaml and fill in base64 values.
# How to use:
# 1) Copy:  cp k8s/secrets/minio-secret.yaml.example k8s/secrets/minio-secret.yaml
# 2) Set secure credentials, then base64 encode them:
#    echo -n 'your-root-user' | base64
#    echo -n 'your-root-password' | base64
# 3) Replace the placeholders below.
# 4) Apply: kubectl apply -f k8s/secrets/minio-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: default
type: Opaque
data:
  # base64(root user)
  rootUser: <base64-root-user>
  # base64(root password)
  rootPassword: <base64-root-password>
