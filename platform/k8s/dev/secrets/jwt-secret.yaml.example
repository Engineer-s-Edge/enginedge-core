# This is an example secret file for JWT configuration.
# DO NOT COMMIT THE ACTUAL SECRET FILE (jwt-secret.yaml) TO VERSION CONTROL.
#
# How to use:
# 1. Copy this file: cp k8s/secrets/jwt-secret.yaml.example k8s/secrets/jwt-secret.yaml
# 2. Choose a secure JWT secret string (32+ random bytes recommended).
# 3. Base64 encode it. For example:
#    echo -n 'your-very-long-random-jwt-secret' | base64
# 4. Replace the placeholder value below with your base64 encoded secret.
# 5. Apply the secret to your cluster:
#    kubectl apply -f k8s/secrets/jwt-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: default
type: Opaque
data:
  # Replace with your base64 encoded JWT secret string
  JWT_SECRET: <your-base64-encoded-jwt-secret>
