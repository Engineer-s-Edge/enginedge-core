# This is an example secret file for Google API credentials used by the scheduling worker.
# DO NOT COMMIT THE ACTUAL SECRET FILE (google-secret.yaml) TO VERSION CONTROL.
#
# How to use:
# 1. Copy this file: cp k8s/secrets/google-secret.yaml.example k8s/secrets/google-secret.yaml
# 2. Obtain your Google service account credentials JSON file from Google Cloud Console
# 3. Base64 encode the entire JSON content:
#    cat your-service-account-key.json | base64 -w 0
# 4. Replace the placeholder value below with your base64 encoded credentials
# 5. Apply the secret to your cluster: kubectl apply -f k8s/secrets/google-secret.yaml
#
# For Google Calendar API access:
# - Create a Google Cloud Project
# - Enable the Google Calendar API
# - Create a Service Account with Calendar API access
# - Download the JSON key file
# - Base64 encode the entire JSON content

apiVersion: v1
kind: Secret
metadata:
  name: google-secret
  namespace: default
type: Opaque
data:
  # Base64 encoded Google service account credentials JSON
  # Example: cat service-account-key.json | base64 -w 0
  google-credentials.json: <BASE64_ENCODED_GOOGLE_SERVICE_ACCOUNT_JSON>

  # Alternative: If using API key instead of service account
  # google-api-key: <BASE64_ENCODED_API_KEY>