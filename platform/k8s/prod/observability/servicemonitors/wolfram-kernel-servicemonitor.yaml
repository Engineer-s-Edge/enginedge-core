apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: wolfram-kernel
  namespace: observability
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: enginedge
      component: wolfram-kernel
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
