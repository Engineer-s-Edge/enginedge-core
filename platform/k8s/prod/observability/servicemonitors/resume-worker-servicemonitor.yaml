apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: resume-worker
  namespace: observability
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: enginedge
      component: resume-worker
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
