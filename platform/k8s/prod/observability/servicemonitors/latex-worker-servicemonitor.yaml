apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: latex-worker
  namespace: observability
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: enginedge
      component: latex-worker
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
