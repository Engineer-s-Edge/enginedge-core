apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: wolfram-kernel-policy
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: enginedge
      component: wolfram-kernel
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: enginedge
          component: main-node
    ports:
    - protocol: TCP
      port: 5000
